import { StyleSheet } from "react-native";

export const styles = () => {
  return StyleSheet.create({
    input: {
      color: "black",
      backgroundColor: "#E3E3FA",
      padding: 20,
      borderRadius: 8,
    },
  });
};
