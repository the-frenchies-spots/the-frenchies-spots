import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  detailsActionContainer: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    flexGrow: 0,
    flexShrink: 1,
    flexBasis: "auto",
  },
  viewInMapButton: {
    flex: 1,
  },
});
