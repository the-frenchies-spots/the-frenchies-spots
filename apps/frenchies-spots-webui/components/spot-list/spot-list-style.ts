import { StyleSheet } from "react-native";
import { ThemeType } from "../../theme";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
  },
  spotCard: {
    // marginBottom: 16,
  },
  col: { marginBottom: 16 },
  loading: {
    width: "100%",
    height: "100%",
  },
});
