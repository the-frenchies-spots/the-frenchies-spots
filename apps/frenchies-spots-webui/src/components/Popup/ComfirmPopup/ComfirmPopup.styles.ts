import { Sx } from "@frenchies-spots/material";

export const styles: Record<string, Sx> = {
  boxPopup: (theme) => ({
    background: theme.white,
    borderRadius: 10,
    boxShadow: theme.shadows.md,
  }),
};
